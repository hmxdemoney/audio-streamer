[package]
name = "audio-streamer"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = { version = "0.7", features = ["ws"] }
tokio = { version = "1", features = ["full"] }
cpal = "0.15"
tower-http = { version = "0.5", features = ["cors"] }